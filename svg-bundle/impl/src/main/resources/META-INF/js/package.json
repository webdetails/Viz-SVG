{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "private": true,
  "description": "Pentaho Visual Samples SVG",
  "license": "MIT",

  "config": {
     "pentaho/typeInfo": {
	  "${project.artifactId}_${project.version}/modelProductLine": {"base": "pentaho/visual/base/model"},
      "${project.artifactId}_${project.version}/modelCar": {"base": "pentaho/visual/base/model"},
      "${project.artifactId}_${project.version}/modelMap": {"base": "pentaho/visual/base/model"},
      "${project.artifactId}_${project.version}/modelOffice": {"base": "pentaho/visual/base/model"},
    },
    "pentaho/instanceInfo": {
      "${project.artifactId}_${project.version}/config": {"type": "pentaho.config.spec.IRuleSet"}
    }
  },

  "dependencies": {
    
  }
}
