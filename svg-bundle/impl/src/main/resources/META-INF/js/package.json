{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "private": true,
  "description": "Pentaho Visual Samples SVG",
  "license": "MIT",

  "config": {
    "pentaho/service": {
	  "${project.artifactId}_${project.version}/modelProductLine": "pentaho/visual/base",
      "${project.artifactId}_${project.version}/modelCar": "pentaho/visual/base",
      "${project.artifactId}_${project.version}/modelMap": "pentaho/visual/base",
      "${project.artifactId}_${project.version}/modelOffice": "pentaho/visual/base",
      "${project.artifactId}_${project.version}/config": "pentaho.config.spec.IRuleSet"
    }
  },

  "dependencies": {
    
  }
}
